# Changelog — StockAgent Front-End

> Maintained automatically by the monitoring assistant. Each entry records changes detected in
> `src/` while the project is edited (e.g. via Antigravity CLI).
>
> Raw file-watch events are appended to `.monitor/changes.log`.
> A full baseline snapshot (hashes + line counts) is stored in `.monitor/baseline.json`.

## Baseline — 2026-08-26

Initial captured state before active monitoring:

- **18 source files**, **~115 KB** total.
- Stack: Vite 8 + React 19 + TypeScript 6 + Tailwind 3 + Oxlint + lucide-react.
- Single `App` shell with 5 tab views (home/features/pricing/docs/about) and an onboarding modal.
- Components under `src/app/components/` (14 files). Largest: `Integrations.tsx` (15 KB),
  `LiveWidgetDemo.tsx` (15 KB), `PricingView.tsx` (8.5 KB).

---

<!-- New entries are appended above this line as changes are detected. -->
